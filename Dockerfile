# Use the jupyter datascience-notebook
FROM jupyter/datascience-notebook

# Left here for adding any python libraries that we need for later on
#RUN pip install nltk svgling dython tensorflow plotly spacy
